/**
 * src/lib/sanity.js — DEPRECATED
 *
 * Sanity CMS has been removed from this project.
 * All content now lives in src/data/*.ts
 *
 * This file is a stub to prevent import errors from any lingering references.
 * It can be deleted safely once all imports are confirmed removed.
 */

export const queries = {};
export const urlFor = () => ({ url: () => '', width: () => ({ height: () => ({ url: () => '' }) }) });
